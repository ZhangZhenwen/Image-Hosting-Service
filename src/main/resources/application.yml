# Tomcat 配置
server:
  port: 7080
  servlet:
    # 应用的访问路径
    context-path: /

# Spring 配置
spring:
  profiles:
    active: prod
  # 文件上传
  servlet:
    multipart:
      # 单个文件大小
      max-file-size: 10MB
      # 设置总上传的文件大小
      max-request-size: 50MB
